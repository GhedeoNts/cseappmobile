
const candidates = (state, { type, payload }) => {

    switch (type) {
        case 'GET_CANDIDATES':
            return state;

        default:
            return state;
    }

}

export default candidates